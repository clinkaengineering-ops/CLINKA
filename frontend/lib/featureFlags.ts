/**
 * Feature toggles — flip to `true` when ready to ship.
 */
export const featureFlags = {
  /** Sidebar Pro upgrade card → settings / billing (not implemented yet). */
  proUpgrade: false,
} as const;
